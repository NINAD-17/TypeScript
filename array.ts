const superHeros: string[] = [];
const heroPower: number[] = [];
const heroHealth:Array<number> = [];

type User = {
    name: string
    isActive: boolean
}

const allUsers: User[] = [];

const MLmodels: number[][] = [
    [255, 255, 255],
    []
]

superHeros.push("spiderman");
heroPower.push(12);

allUsers.push({
    name: "ninad",
    isActive: true
})